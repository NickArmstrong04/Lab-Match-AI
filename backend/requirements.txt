fastapi
uvicorn
bcrypt
pyjwt
pydantic
python-multipart
supabase
pydantic-settings
pypdf
apscheduler
google-auth-oauthlib
google-api-python-client
google-auth
# MX/A lookups in services/contact_validation.py. Pure Python, no transitive deps.
# socket.getaddrinfo cannot distinguish NXDOMAIN from a resolver timeout, and that is
# exactly the line between withdrawing an address and leaving it served -- see
# domain_deliverability(). Optional at runtime: the import is guarded, and without it
# every address stays 'unknown' rather than anything being withdrawn.
dnspython
